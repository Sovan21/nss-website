import en from './en.json';
import bn from './bn.json';
import hi from './hi.json';

export const translations = {
  en,
  bn,
  hi,
};

export const defaultLocale = 'en';
export const supportedLocales = ['en', 'bn', 'hi'];
